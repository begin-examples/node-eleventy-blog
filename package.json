{
  "name": "node-eleventy-blog",
  "version": "1.0.0",
  "description": "An example Eleventy blog",
  "scripts": {
    "build": "eleventy",
    "watch": "eleventy --serve",
    "start": "run-p watch sandbox",
    "sandbox": "sandbox",
    "test": "tape test/*-test.js test/**/*-test.js | tap-arc"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@11ty/eleventy": "^0.12.1",
    "@architect/sandbox": "latest",
    "@begin/data": "latest",
    "npm-run-all": "^4.1.5",
    "tap-arc": "^0.1.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.2",
    "tape": "^5.0.1",
    "tiny-json-http": "^7.2.2"
  }
}
